import { RollButton } from './RollButton';
import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof RollButton> = {
  component: RollButton
};

export default meta;
type Story = StoryObj<typeof RollButton>;

export const Default: Story = {};
